# 시크릿 관리 기준

작성 2026-09-16 · 갱신 2026-09-16(RDS Proxy 전환 시점·`rotated-on` 태그 추기) · 갱신 2026-09-17(회전 면제 태그 `rotation-exempt` 추기) · 적용 대상: AWS 계정 972934292912의 AWS Secrets Manager 시크릿 전체와 이를 사용하는 서비스·배포·개발 환경 · 근거 정책: 시크릿 회전 정책 수행계획서(APV-2026-096, 2026-09-09 승인)

## 1. 목적

자격증명의 원본을 AWS Secrets Manager 한 곳으로 통일하고, 모든 사용처가 값이 아니라 이름으로 참조하게 한다. 이 상태가 되어야 회전이 사용처 조사 없이 반복 가능해진다. 2026-09-14 전역 사용처 조사에서 관리자 비밀번호 동일 값이 141곳, 환경변수 묶음 시크릿의 값 사본이 최대 170곳에서 발견됐다. 이 기준은 그 상태를 되돌리고 재발을 막기 위한 것이다.

## 2. 원칙

1. **자격증명 하나 = 시크릿 하나.** DB 계정, API 키, 서명 키 각각을 전용 시크릿으로 둔다. 이름은 `<서비스>/<용도>` 형식으로 하고, 생성 시 `rotation-class`·`rotation-days`·`owner`·`consumer-mode` 태그를 함께 넣는다. 자격증명 값을 바꾼 날에는 `rotated-on`(YYYY-MM-DD, KST 날짜) 태그를 갱신한다. 태그의 값과 필수 여부는 아래 표를 따른다.
2. **환경변수 묶음 시크릿에는 값 사본을 넣지 않는다.** 여기서 묶음 시크릿이란 로컬 `.env` 파일이 아니라 Secrets Manager 안에서 서비스 환경변수 전체를 JSON 하나로 담고 있는 `prd.*/env` 이름의 시크릿(현재 14개)을 말한다. 다른 시크릿의 값을 복사해 넣는 대신 그 시크릿의 이름을 넣고, 애플리케이션이 이름으로 조회한다. 묶음 안에 남길 수 있는 것은 비밀이 아닌 구성값뿐이다.
3. **사람이 값을 복사해 넣는 경로는 만들지 않는다.** 로컬 env 파일, 배포 스크립트의 리터럴, 코드 하드코딩, 타 시크릿 안의 복사본 모두 해당한다.
4. **참조 방식은 3절의 두 가지만 허용한다.** 어느 쪽인지 시크릿의 `consumer-mode` 태그에 기록한다.
5. **Secrets Manager 밖의 저장소는 파생 사본으로 취급한다.** 4절의 절차 없이는 두지 않는다.

시크릿에 붙이는 태그는 다음 일곱 가지다.

| 태그 | 값 | 필수 여부 | 용도 |
|---|---|---|---|
| `rotation-class` | `1a`, `1b`, `1c`, `2`, `3`, `4`, `5`, `6`, `7` | 생성 시 필수 | 5절의 회전 등급 |
| `rotation-days` | `90`, `180`, `365`, `730`, `component`, `none` | 생성 시 필수 | 회전 주기. `component`는 상위 시크릿의 구성요소, `none`은 회전 대상이 아님 |
| `owner` | 담당자 | 생성 시 필수 | 표시용. 점검 판정에는 쓰지 않는다 |
| `consumer-mode` | `A`, `B` | 생성 시 필수 | 3절의 참조 방식 |
| `rotated-on` | YYYY-MM-DD(KST 날짜) | 회전 후 필수 | 자격증명 값을 바꾼 날. 주간 점검의 회전 나이 기준일 |
| `rotation-exempt` | 결재 번호(`APV-YYYY-NNN`) | 선택 | 회전을 면제한 결재 번호. 이 값이 면제의 근거다 |
| `rotation-exempt-until` | YYYY-MM-DD(KST 날짜) | 선택 | 면제 만료일. 없으면 기한 없는 면제 |

주간 점검은 표에 있는 태그의 규약 밖 값과, `rotation-`으로 시작하는데 표에 없는 태그 이름을 오타로 보아 태그 누락·무효로 보고한다. `owner`와 `consumer-mode`는 표시용이라 값으로 판정하지 않고, 접두가 다른 그 밖의 태그는 점검 대상이 아니다.

## 3. 허용하는 참조 방식

| 방식 | 동작 | 회전 시 반영 | 적합한 대상 |
|---|---|---|---|
| **A. 실행 시 조회** | 애플리케이션이 실행 중 SDK로 GetSecretValue를 호출한다. 결과를 캐시하되 만료 시간을 둔다(기준 5분). 키 검증 측은 AWSCURRENT와 AWSPREVIOUS를 모두 수용한다(기준 7일). | 캐시 만료 후 자동 | 내부 API 키, Lambda·Batch 잡, 요청 단위로 자격증명을 쓰는 코드 |
| **B. 시작 시 주입** | ECS 태스크 정의의 `secrets`, Batch 잡 정의, Lambda 환경변수의 시크릿 이름으로 프로세스 시작 시 값을 받는다. 프로세스 수명 동안 값은 고정된다. | 재배포·재시작이 필요하다. 회전 절차에 재배포 단계를 반드시 포함한다. | DB 연결 풀처럼 실행 중 교체가 어려운 자격증명 |

- A 방식 구현은 언어별 공용 로더를 쓴다. Node.js는 `apps/backoffice-api/src/lib/rotating-secret.ts`가 기준 구현이다. 다른 서비스와 언어는 같은 동작(만료 캐시·이전 버전 수용)을 갖춘 로더를 만들어 공유한다.
- 환경변수 값이 있으면 Secrets Manager를 건너뛰는 폴백은 로컬 개발 용도로만 허용하고, 운영 배포 설정에는 그 환경변수를 두지 않는다.
- RDS Proxy 인증 항목은 프록시가 시크릿을 스스로 재조회하므로 A 방식으로 본다.

## 4. Secrets Manager 밖의 저장소

Vercel 환경변수, GitHub Actions 시크릿, 외부 호스트(GPU 서버 등)의 환경 파일, 개발자 Mac의 설정 파일은 Secrets Manager를 원본으로 삼을 수 없다. 이 경우 다음을 지킨다.

1. 해당 값의 전용 시크릿이 Secrets Manager에 먼저 있어야 한다. 그 시크릿이 원본이고 밖의 값은 사본이다.
2. 사본의 위치와 갱신 절차를 전용 시크릿의 `Description` 또는 서비스 런북에 적는다. 적히지 않은 사본은 존재하지 않는 것으로 간주하고 회전 때 고려하지 않는다.
3. 회전 절차는 "원본 갱신 → 사본 갱신 → 프로세스 교체 → 구 값 폐기" 순서로 문서화하고, 구 값 폐기 전에 사본 갱신을 확인한다.
4. 배포 파이프라인이 있는 곳(Vercel, GitHub Actions)은 배포 시점에 Secrets Manager에서 값을 가져오는 방식으로 사본을 없애는 것을 우선한다.

## 5. 회전 방법

| 등급 | 대상 | 방법 |
|---|---|---|
| 1a 관리자 DB 계정 | RDS 관리자 | 수동. MySQL 이중 비밀번호(RETAIN CURRENT PASSWORD)로 새 값 등록 → 시크릿 갱신 → 사본·소비자 확인 → 구 값 폐기(DISCARD OLD PASSWORD). 동일 값을 공유하는 시크릿을 한 세트로 다룬다. **이중 비밀번호의 유예는 DB 인스턴스 직결에만 적용된다.** RDS Proxy는 클라이언트가 낸 비밀번호를 등록된 시크릿의 현재 값과만 대조하므로, 시크릿을 갱신하는 순간부터 프록시 경유 연결은 구 값을 거부한다(2026-09-16 실측, 갱신 후 약 60초 내 반영). 프록시 경유 소비자가 구 값을 캐시하고 있으면 시크릿 갱신 시점이 곧 전환 시점이므로, 그 소비자의 갱신·재배포를 시크릿 갱신 직후에 붙인다. |
| 1b 응용 DB 계정 · 1c RDS Proxy 계정 | 서비스 전용 DB 사용자 | 수동 절차는 1a와 같다. 사용처가 A 방식으로 정리된 계정부터 Secrets Manager 회전 기능(회전 Lambda) 적용을 검토한다. 회전 Lambda는 프록시를 거치지 않고 DB 인스턴스에 직접 접속한다. |
| 2 IAM 키 포함 | 외부 호스트용 액세스 키 | IAM에서 새 키 발급 → 시크릿 갱신 → 4절 사본 갱신 → 구 키 비활성화 → 삭제 |
| 3 외부 발급 키 | 결제·플랫폼·API 키 | 발급처에서 새 키 발급 → 시크릿 갱신 → 소비자 반영 → 발급처에서 구 키 폐기 |
| 4 내부 서비스 간 키 | 서비스 간 인증 키 | A 방식 전제. 새 값을 AWSCURRENT로 저장하면 검증 측이 7일간 신·구 모두 수용하고, 호출 측은 캐시 만료 후 새 값을 쓴다. 7일 후 이전 버전 수용을 종료한다. |
| 5 암호화·서명 키 | JWT·AES 키 | 기존 데이터·토큰의 복호화 의존성을 먼저 확인한다. 키 버전을 병행하는 설계가 없으면 회전하지 않고 보류 사유를 기록한다. |
| 6 환경변수 묶음 | `prd.*/env` | 회전 대상이 아니다. 2절에 따라 값 사본을 제거하는 것이 조치다. |

회전 완료의 판정은 다음 세 가지를 모두 확인한 시점이다. 새 값으로 실제 인증 성공, 구 값으로 인증 거부, 회전 후 해당 계정의 인증 실패 로그 0건. 완료 시 시크릿의 `rotated-on` 태그를 값을 바꾼 날짜(KST)로 갱신한다. 이 태그가 주간 점검의 회전 나이 기준이므로 빠뜨리면 다음 회전 기한이 잘못 계산된다.

## 6. 점검 장치

- **주간 점검 Lambda(`backoffice-isms-secret-rotation`, 매주 월 09:00 KST, Slack #security)**: 회전 기한 초과와 태그 누락을 게시한다. 회전 나이의 기준은 시크릿의 LastChangedDate나 최신 버전(AWSCURRENT) 생성일이 아니라 **자격증명 값이 실제로 바뀐 날짜**다. 최신 버전 생성일은 태그·설명·스키마 변경으로도 리셋되므로 쓰지 않는다(2026-09-16 실측: 4건이 스키마 변경으로 "이틀 전 회전"으로 잘못 집계됨). 점검 Lambda는 값을 읽지 않으므로 기준일을 `RotationEnabled`이고 LastRotatedDate가 있는 건(자동 회전이 한 번 이상 실행된 건)은 LastRotatedDate, 그 외는 `rotated-on` 태그(KST 날짜)에서 읽고, 태그가 없으면 시크릿 생성일을 쓰며 "회전 기록 없음"으로 표기한다. 2026-09-16 기준일 백필은 버전별 값 해시 대조로 산출했다.
- **회전 면제**: 면제 건은 주간 점검의 주기 초과·만료 임박 판정에서 제외하고, 보고서에 결재 번호와 함께 별도 항목으로 표기한다. 면제는 결재 문서로만 부여하고 `rotation-exempt` 태그 값은 그 결재 번호다. 결재 번호 형식이 아니거나, `rotation-exempt-until` 만료일이 지났거나, 만료일 형식이 `YYYY-MM-DD`가 아니거나, 결재 번호 없이 만료일만 달린 시크릿은 면제로 보지 않고 태그 누락·무효로 보고한다. 회전 대상이 아닌 시크릿(`rotation-days`가 `component` 또는 `none`)에 면제 태그를 다는 것도 태그 오용으로 보고한다. 유효한 면제 건에는 `rotated-on` 태그를 요구하지 않는다. 이번 주기를 돌지 않기로 결재된 건이기 때문이다. 기한을 둔 면제는 만료일이 지나면 다시 회전 대상이 되므로, 만료 전에 회전하거나 새 결재를 받아 태그를 갱신한다.
- **값 중복 검사(추가 예정)**: 같은 점검 Lambda가 시크릿 간 자격증명 값의 해시를 대조해 사본이 생기면 게시한다. 이 검사는 GetSecretValue를 호출하므로 LastAccessedDate를 사용 여부 판정에 쓰지 않는다.
- **사용 여부 판정**: CloudTrail의 GetSecretValue 이벤트를 호출 주체별로 분류해 판정한다. 시크릿별 조회는 ARN과 이름 두 형식으로 해야 누락이 없다.
- **회전 후 감시**: DB 계정은 RDS 오류 로그의 해당 사용자 `Access denied`, 내부 키는 검증 측의 인증 거부 건수를 회전 후 하루 동안 본다.

## 7. 적용 순서

1. 회전 대상으로 고른 시크릿마다 이 기준으로 사용처를 정리한 뒤 회전한다. 전체를 한 번에 바꾸지 않는다.
2. 환경변수 묶음 14개의 값 사본 제거는 서비스별 코드 변경이 필요하므로 별도 작업으로 계획한다. 우선순위는 관리자 DB 비밀번호가 들어 있는 묶음부터다.
3. 신규 시크릿과 신규 서비스는 생성 시점부터 이 기준을 적용한다. 코드 리뷰에서 2절·3절 위반을 확인한다.

## 8. 용어

- **원본**: Secrets Manager의 전용 시크릿. 회전은 항상 원본에서 시작한다.
- **사본**: 원본과 같은 값을 가진 다른 저장 위치. 회전 시 함께 갱신해야 한다.
- **이름 참조**: 값이 아니라 시크릿 이름 또는 ARN을 저장하는 것. 회전 시 갱신이 필요 없다.
- **AWSCURRENT / AWSPREVIOUS**: Secrets Manager의 현재 버전과 직전 버전 라벨. 4등급 키의 신·구 동시 수용은 이 두 버전을 읽어 구현한다.
